
server {
	listen 443 ssl;
	listen 80;
	# ssl on;
	# ssl_certificate /etc/nginx/certs/live/heliumlabs.org/fullchain.pem;
	# ssl_certificate_key /etc/nginx/certs/live/heliumlabs.org/privkey.pem;

	# ssl_session_cache shared:le_nginx_SSL:1m;
	# ssl_session_timeout 1440m;
	# ssl_protocols TLSv1 TLSv1.1 TLSv1.2;
	# ssl_prefer_server_ciphers on;
	# ssl_ciphers 'ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-SHA384:ECDHE-RSA-AES256-SHA384:ECDHE-ECDSA-AES128-SHA256:ECDHE-RSA-AES128-SHA256';

	server_name api.heliumlabs.org;
	location / {
		proxy_pass http://app:3000;
		proxy_http_version 1.1;
		proxy_set_header Upgrade $http_upgrade;
		proxy_set_header Connection 'upgrade';
		proxy_set_header Host $host;
		proxy_cache_bypass $http_upgrade;
	}
}

#server {
#	listen 80;
#	server_name *.heliumlabs.org;
#	location /.well-known/acme-challenge/ {
#		root /var/www/letsencrypt;
#	}
#	location / {
#		return 301 https://$host$request_uri;
#	}
#}

server {
	listen 80;
	server_name heliumlabs.org;
	return 301 https://www.heliumlabs.org$request_uri;
}